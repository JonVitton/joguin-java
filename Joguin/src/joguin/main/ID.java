package joguin.main;

/*Enum (Enumeration) ermite que criemos valores para determinadas classes
 * fazendo com que seja possivel o programa diferenciar o jogador de inimigos
 * e objetos no cenário.
 * */

public enum ID {
	
	Player(),
	Inimigo();

}
